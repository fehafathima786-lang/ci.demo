name: CI Pipeline 
on: push
jobs:
   Build:
      run-on: ubuntu-latest
      
      step:
        - name: Checkout code
        uses: actions/checkout@v3 

        - name: Run Python Script
        run: python hello.py

        
      
